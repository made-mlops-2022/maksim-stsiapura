apiVersion: v1
kind: Pod
metadata:
  name: online-inference
  labels:
    app: online-inference
spec:
  containers:
    - image: maksima1ist/online_inference:1.0.2
      name: online-inference
      ports:
        - containerPort: 5000